# Research run history

Every `RUN_XXXX_*` directory is an immutable research snapshot.
`index.json` is the chronological run catalog.
Each run stores compact results plus code/config snapshots.
Raw market data is not committed.
